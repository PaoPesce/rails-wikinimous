<h1>ARTICLES</h1>

<% @articles.each do |article| %>
<h3><%= link_to "#{article.title}", article_path(acticle) %> - <%= article.completed ? 'Done' : 'To do' %></h3>
<% end %>

<%= link_to 'New Article', new_article_path  %>
